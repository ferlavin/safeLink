# URL base del backend FastAPI
# Desarrollo: dejá vacío y Vite usa el proxy /api → localhost:8000 (ver vite.config.js)
# Producción (Vercel): https://tu-api.onrender.com (misma URL que extension/config.js)
VITE_API_URL=
